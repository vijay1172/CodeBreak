// Canonical production origin. Every absolute URL (SEO metadata, sitemap,
// structured data) derives from this constant so the domain lives in one place.
export const SITE_URL = "https://brokenrepo.tech";
